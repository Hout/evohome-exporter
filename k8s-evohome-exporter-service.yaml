apiVersion: v1
kind: Service
metadata:
  name: evohome-exporter
  labels:
    app: evohome-exporter
  namespace: evohome-exporter
spec:
  type: ClusterIP
  ports:
    - port: 8082
  selector:
    app: evohome-exporter
