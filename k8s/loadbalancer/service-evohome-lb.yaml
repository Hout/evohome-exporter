apiVersion: v1
kind: Service
metadata:
  name: evohome-svc
  namespace: evohome
  labels:
    app: evohome-exporter
  annotations:
    metallb.universe.tf/allow-shared-ip: shared
spec:
  selector:
    app: evohome-exporter
  type: LoadBalancer
  loadBalancerIP: 192.168.1.60
  ports:
    - name: metrics
      port: 8082
